// basically will put all the variables that should be constants and should be reuse across the project.
// The goal is to allow us easy configuer the project by simply changing some of the data in config.js file
// config.env for backend

export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';
export const TIMEOUT_SEC = 10;
